name: Apply V1 tags

on:
  push:
    branches:
      - agent/v1-tags
    paths:
      - .github/apply_v1_tags.py
      - .github/workflows/apply-v1-tags.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1-tags
      - name: Apply implementation
        run: python .github/apply_v1_tags.py
      - name: Remove temporary patch files
        run: rm .github/apply_v1_tags.py .github/workflows/apply-v1-tags.yml
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "V1: implement encrypted tags"
          git push origin HEAD:agent/v1-tags
