name: Apply final responsive shell polish

on:
  push:
    branches:
      - agent/v1-responsive-shell-save-fix

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1-responsive-shell-save-fix
      - run: python .github/final_shell_polish.py
      - run: |
          rm .github/final_shell_polish.py
          rm .github/workflows/final-shell-polish.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: align fluid shell navigation boundaries"
          git push origin agent/v1-responsive-shell-save-fix
